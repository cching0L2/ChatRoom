mixin toolbar(user)
  #toolbar
    div.right
      if user 
        div.welcome
          h4.msg Hello, #[span.username= user.username + ' !']
      div.buttons
        button#register #[a(href="/auth/register") Register] 
        button#login #[a(href="/auth/login") Login] 
        button#logout #[a(href="/auth/logout") Logout] 
      
